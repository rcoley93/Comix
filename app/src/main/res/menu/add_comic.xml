<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- Search / will display always -->
    <item
        android:id="@+id/cancel_comic"
        android:icon="@android:drawable/ic_menu_close_clear_cancel"
        android:title="@string/cancel_comic"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/barcode_comic"
        android:icon="@drawable/barcode"
        android:title="@string/barcode_comic"
        app:showAsAction="ifRoom" />

    <!-- Location Found -->
    <item
        android:id="@+id/save_comic"
        android:icon="@android:drawable/ic_menu_save"
        android:title="@string/save_comic"
        app:showAsAction="ifRoom" />
</menu>